import {
    // style sheets:
    styleSheet,
    
    
    
    // scopes:
    scopeOf,
    mainScope,
    globalScope,
} from '../src/cssfn'



styleSheet(() => [
    mainScope(
    ),
    scopeOf('menuBar',
    ),
    globalScope(
    ),
]);
