Optimize flat(), unwrap(), filter(), map():
flatExisting
flatMapExisting

Optimize mergeSelectors => pure*** => ***
Fix bug on css-config when renaming `cssConfig.prefix`

add auto-id on cssfn-react createStyleSheet(s)



optimize:

export const encodeStyle = (style) => {
    ...
    const encodedStyle = Object.fromEntries(Object.entries(styleValue) // take all string keys (excluding symbol keys)
    ...
    const symbolProps = Object.getOwnPropertySymbols(styleValue); // take all symbol keys
    ...
}

export const alwaysRule = (styles) => ({ [Symbol()]: [['&', undefined], styles] }); // a bit faster

#findEqualProp(srcPropName, srcPropValue) {
    ...
    for (const [refPropName, refPropValue] of this.#refProps) { // search for duplicates
    ...
}

combines requestAnimationFrame with setTimeout